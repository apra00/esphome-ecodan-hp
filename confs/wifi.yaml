wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  output_power: 11dB

  power_save_mode: high
  reboot_timeout: 0s
  ap:
    ssid: "${name}"
    password: "configesp"
    ap_timeout: 15s

<<: !include wifi-base.yaml
